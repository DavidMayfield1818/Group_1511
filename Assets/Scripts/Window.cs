using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Window : MonoBehaviour
{
    // Start is called before the first frame update
    public GameObject hoopR;
    void Start()
    {
        // hoopR = GameObject.Find("hoopRight");
        // hoopR.OnMouseOver = () => PercentHover.ShowHover_Static(90);
        // hoopR.OnMouseExit = () => PercentHover.HideHover_Static();
        // transform.Find("hoopRight").GetComponent<Hover>().OnMouseOver = () => PercentHover.ShowHover_Static(90);
        // transform.Find("hoopRight").GetComponent<Hover>().OnMouseExit = () => PercentHover.HideHover_Static();
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
